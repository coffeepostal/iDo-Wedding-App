<%- guest = @guest || @session_guest -%>

<h1>Update <%= @admin_user ? "#{guest.first_name}’s" : 'Your' %> Info</h1>

<%- form_for guest, :url => @admin_user ? admin_guest_path(guest) : guest_path do |form| -%>
	<%= render form %>
	<p><%= form.submit 'Update' %></p>
<%- end -%>

<%= link_to 'Back', @admin_user ? admin_guest_path(guest) : guest_path %>
